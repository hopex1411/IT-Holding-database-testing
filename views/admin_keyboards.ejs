<% include ./partials/admin_header %>

    <section class="admin_content">
        <h1>Admin Panel | Keyboards</h1>
        <form method="POST" enctype="multipart/form-data" class="admin_form_create_update">
            <legend>Add Keyboard</legend>
            <fieldset>
            <label for="brand">Brand: </label>
            <input type="text" name="brand" id="brand" required>
        </fieldset>
        <fieldset>
            <label for="model_number">Model Number: </label>
            <input type="text" name="model_number" id="model_number" required>
        </fieldset>
        <fieldset>
            <label for="connections">Connection: </label>
            <input type="text" name="connections" id="connections" required>
        </fieldset>
        <button type="submit">Add</button>
        <small class="error_message"><%= (typeof errorMessage != 'undefined') ? errorMessage : '' %></small>
        </form>

        <table class="admin_read_data_table">
            <thead>   
                <tr>
                    <th></th>
                    <th>Brand</th>
                    <th>Model Number</th>
                    <th>Connection</th>
                </tr>
            </thead>
            <tbody>
                <% results.forEach(result => { %>
                    <tr>
                        <td>
                            <a class="admin_edit" href="/editKeyboards/<%= result.id %>">Rediger</a>
                            <a class="admin_delete" href="/deleteKeyboards/<%= result.id %>">Slet</a>
                        </td>
                        <td><%= result.brand %></td>
                        <td><%= result.model_number %></td>
                        <td><%= result.connections %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </section>

<% include ./partials/admin_footer %>